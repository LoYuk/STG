using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameBG_Camera : MonoBehaviour
{

    // Start is called before the first frame update
    void Start()
    {
        var BGCamera = Camera.main;
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
